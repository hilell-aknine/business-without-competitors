# אודיט פורטל — 2026-05-25

> בדיקה מקיפה של כל הכפתורים, מודולים, ניווט ו-flows. 6 סוכנים במקביל, read-only.
> סה"כ נמצאו: **3 חוסמים · 12 גבוהים · 8 בינוניים · 6 נמוכים**

---

## 🚨 חוסמים (BLOCKER) — חייב לתקן מיד

### B1. במובייל אי אפשר לפתוח את התפריט של השיעורים
- **איפה:** `index.html` — חסר כפתור `#sidebarToggle`
- **מה קורה:** במסך מובייל ה-sidebar נסגר מהצד, ואין שום כפתור המבורגר לפתוח אותו. משתמש במובייל **לא יכול להגיע לאף שיעור** מלבד "הבא".
- **תיקון:** להוסיף כפתור המבורגר ל-`js/global-nav.js` או ל-`index.html`. ⏱️ ~20 דקות
- **חומרה:** קריטי — כ-60% מהמשתמשים גולשים ממובייל

### B2. דשבורד "ההתקדמות שלי" מציג 0/132 לכולם
- **איפה:** `pages/progress.html:329, 347-362, 364-375, 452-456`
- **מה קורה:** הקוד קורא ל-`bwc_completed` כ-object אבל הוא נשמר כ-array. כל אורח רואה 0 שיעורים הושלמו ו-0% בכל מודול, גם אם השלים 50 שיעורים.
- **תיקון:** להמיר ל-`new Set(arr).has(key)` במקום `obj[key]`. ⏱️ ~30 דקות
- **חומרה:** קריטי — מבטל את כל הערך של הדשבורד

### B3. כפתור "מחיקת חשבון" לא עובד
- **איפה:** `pages/profile.html:947` — קורא ל-`delete_user` RPC שלא קיים בסופבייס
- **מה קורה:** כל לחיצה על "מחק חשבון" מציגה "לא ניתן למחוק את החשבון כרגע. פנה אלינו ישירות". פיצ'ר מוצג אבל לא עובד בכלל.
- **תיקון:** ליצור פונקציית `delete_user()` ב-Supabase migration, או להוריד את ה-UI עד שיהיה. ⏱️ ~45 דקות
- **חומרה:** קריטי משפטית — GDPR/חוק הגנת הפרטיות מחייב מחיקה אפקטיבית

---

## 🔴 גבוה (HIGH) — לתקן השבוע

### H1. כפתור "סטטיסטיקה: במודול הנוכחי" מוביל ל-404
- **איפה:** `index.html:2163` — שולח `?module=N` אבל `module.html` קורא `?id=N`
- **מה קורה:** כל לחיצה על הקובייה השמאלית מגלגלת לדף הספרייה במקום למודול
- **תיקון:** להחליף `?module=` ל-`?id=`. ⏱️ 2 דקות

### H2. כפתור "דלג לשיעור הבא" מנגן את אותו שיעור כמו ה-CTA הראשי
- **איפה:** `index.html:1466-1468` — שני הכפתורים קוראים ל-`playByItem(next)` עם אותו `next`
- **מה קורה:** הכפתור משקר — לא מדלג כלום
- **תיקון:** לחשב `nextAfter` ולקרוא לו, או להחביא את הכפתור כשאין מה לדלג. ⏱️ ~15 דקות

### H3. השלמת שיעור לא מסונכרנת ל-Supabase
- **איפה:** `index.html:1933-1959` — `markCurrentComplete` כותב רק ל-localStorage
- **מה קורה:** משתמש שלמד 10 שיעורים במחשב, מתחבר במובייל — רואה 0 שיעורים. הסנכרון רק חד-פעמי בכניסה ראשונה.
- **תיקון:** להוסיף `bwcSupabase.from('course_progress').upsert(...)` אחרי שמירה ל-localStorage. ⏱️ ~20 דקות

### H4. הערות לא מסונכרנות בכלל ל-Supabase
- **איפה:** `js/sync-localstorage.js` (חסר) + `pages/profile.html:834` (קורא מפתח לא קיים)
- **מה קורה:** הערות אישיות נשמרות רק במחשב הזה. החלפת מכשיר/ניקוי דפדפן = איבוד מוחלט של ההערות. גם הייצוא ב"פרופיל" יורד תמיד ריק.
- **תיקון:** להוסיף scan של `bwc_notes_*` ב-sync + upsert בכל שמירת הערה + לתקן הייצוא. ⏱️ ~40 דקות

### H5. `module.html` קורס אחרי redirect לפרמטר לא תקין
- **איפה:** `pages/module.html:78-83` — ה-redirect לא עוצר את ריצת הקוד
- **מה קורה:** קישור עם `?id=abc` מבצע redirect אבל בינתיים זורק TypeError בקונסול. במובייל לפעמים נראה פריים שבור.
- **תיקון:** להוסיף `return` או `throw` אחרי ה-redirect. ⏱️ 2 דקות

### H6. ספירת ניסיונות במבחנים מתנפחת בכל פעם
- **איפה:** `pages/quiz.html:858` + `pages/progress.html:465`
- **מה קורה:** כל merge עם Supabase מסכם `local + remote`, וה-local כבר כולל את ה-remote. עוד טעינה = עוד פעם. אחרי שבוע השדה `attempts` יציג מספרים פי 10 מהאמת.
- **תיקון:** `Math.max(prev.attempts, row.attempts)` במקום סכימה. ⏱️ ~10 דקות

### H7. רצף תרגול (streak) קם לתחייה אחרי שנשבר
- **איפה:** `js/practice.js:139-169` — `current_streak` לא נבדק מול `lastDate`
- **מה קורה:** משתמש שלא תרגל יומיים — אם נכנס במכשיר אחר, ה-merge מחזיר את ה-streak הישן וכותב אותו חזרה כ"קיים".
- **תיקון:** לאפס `current_streak` ל-0 לפני upsert אם `lastDate` ישן מאתמול. ⏱️ ~20 דקות

### H8. מעבר במבחן לא נספר כהשלמת שיעור
- **איפה:** `pages/quiz.html` — `saveScore` לא כותב ל-`course_progress`
- **מה קורה:** מי שעבר את כל 8 המבחנים ולא צפה בסרטונים — הדשבורד מציג 0/132 שיעורים. אין קרדיט.
- **תיקון:** להוסיף upsert ל-`course_progress` עם דגל `via_quiz`. ⏱️ ~20 דקות

### H9. דף "שכחתי סיסמה" שבור על GitHub Pages
- **איפה:** `js/auth-modal.js:228-231` — בונה URL מוחלט שמתעלם מ-subfolder
- **מה קורה:** משתמש על GH Pages לוחץ "שכחתי סיסמה" → מקבל מייל עם קישור ל-`/pages/reset-password.html` במקום `/business-without-competitors/pages/reset-password.html`. 404.
- **תיקון:** להשתמש ב-`getBasePath()` כמו בנבבר. ⏱️ ~10 דקות

### H10. `reset-password.html` חסר סקריפט `auth-modal.js`
- **איפה:** `pages/reset-password.html:392-395`
- **מה קורה:** אם אורח נוחת בדף הזה ולוחץ "התחברות" בנבבר — שום מודאל לא נפתח. רק console error.
- **תיקון:** להוסיף `<script src="../js/auth-modal.js" defer>`. ⏱️ 1 דקה

### H11. השלמה אוטומטית אחרי 45 שניות גם אם המשתמש לא צופה
- **איפה:** `index.html:1907-1908` — `setTimeout` רץ בלי קשר ל-state של הוידאו
- **מה קורה:** משתמש פותח שיעור, עובר לטאב אחר ל-45 שניות, חוזר → השיעור מסומן כהושלם. "צפה" בו בלי לראות.
- **תיקון:** להשתמש ב-YouTube IFrame API לזהות `PLAYING` בפועל, או לעצור את הטיימר ב-`visibilitychange`. ⏱️ ~30 דקות

### H12. `bwcAuth` נעלם בשקט אם Supabase לא נטען
- **איפה:** `js/auth.js:26-29` — `return` בלי לחשוף stub
- **מה קורה:** רשת חלשה / ad-blocker → `pages/profile.html` נכנס ללולאת polling אינסופית. הנבבר לא יודע לדווח שגיאה.
- **תיקון:** לחשוף `window.bwcAuth` כ-stub עם פונקציות no-op שמחזירות שגיאה ברורה. ⏱️ ~15 דקות

---

## 🟡 בינוני (MEDIUM) — סדר עדיפויות שני

| # | בעיה | קובץ | זמן |
|---|------|------|-----|
| M1 | פריטי תפריט "עוד" אף פעם לא מודגשים בזהב | `js/global-nav.js:79-91` | 15 דק |
| M2 | סדר טעינת סקריפטים שגוי ב-practice.html (קורה לרוץ לפני Supabase) | `pages/practice.html:216-218` | 5 דק |
| M3 | חיפוש בספרייה לא תופס תגיות, רק כותרות | `js/library.js:182` | 5 דק |
| M4 | switchVariant מזריק JSON שלם ל-onclick — שביר | `index.html:1593` | 30 דק |
| M5 | שינוי סיסמה בפרופיל לא דורש את הסיסמה הישנה — סיכון security | `pages/profile.html:791` | 20 דק |
| M6 | `closeVideo` משאיר את הדף בחצי-state | `index.html:902, 1918` | 15 דק |
| M7 | reset-password.html לא בודק שיש session אחרי `ready()` | `pages/reset-password.html:478` | 10 דק |
| M8 | בכניסה ראשונה אחרי כשל סנכרון, ה-sync חוזר על עצמו בכל token refresh | `js/sync-localstorage.js:153` | 15 דק |

---

## 🟢 נמוך (LOW) — לתקן בהמשך

| # | בעיה | זמן |
|---|------|-----|
| L1 | קוביית "במודול הנוכחי" כשהכל הושלם → מובילה למודול האחרון (מבלבל) | 5 דק |
| L2 | שורות במודולים בדשבורד התקדמות לא קליקביליות | 10 דק |
| L3 | קוד מת ב-`practice.js` — מודולים נעולים שכבר לא קיימים | 5 דק |
| L4 | מספר "16 חלקים" hard-coded ב-seminars.html (גם JS כותב את אותו דבר) | 2 דק |
| L5 | פונקציה `startModule()` ב-module.html שלא מחוברת לכלום | 5 דק |
| L6 | dead IDs ב-index.html (announceSection, heroEyebrow, וכו') | 10 דק |

---

## ✅ מה עובד מצוין (verified clean)

- כל קישורי הנבבר הראשיים מובילים לקבצים אמיתיים
- הלוגו תמיד מחזיר לדף הראשי
- `getBasePath()` עובד נכון על Vercel, GH Pages, ו-localhost
- `.vercelignore` חוסם את כל מה שצריך (CLAUDE.md, supabase/, PDFs, ZIPs)
- אין absolute paths שבורים על GH Pages
- אין דפים חסרים או href שבורים
- אין duplicate IDs
- כל 8 המודולים + 7 הסמינרים + 132 השיעורים קיימים ב-`course-data.js`
- מנגנון 8 המבחנים × 5 שאלות עובד תקין
- 72 אתגרי תרגול (9 לכל מודול) קיימים ועובדים
- ה-null-guard ב-`renderHero` (commit 117c932) שלם ועובד
- כל 5 הטאבים ב-index.html (about/ai/resources/notes/quiz) קיימים עם פאנלים תואמים
- הגנת ה-auth של profile.html תקינה
- merge logic של completion list בספרייה — נכון

---

## 📋 משימות לביצוע — לפי עדיפות

### חבילה 1 — חוסמים (1-2 שעות) ← להתחיל מכאן
- [ ] B1: כפתור המבורגר במובייל לפתיחת ה-sidebar
- [ ] B2: תיקון `bwc_completed` כ-array בדשבורד התקדמות
- [ ] B3: יצירת RPC `delete_user` ב-Supabase (או הורדת ה-UI)

### חבילה 2 — תיקונים מהירים (30 דקות סה"כ)
- [ ] H1: `?module=` → `?id=` ב-index.html:2163
- [ ] H5: `return` אחרי redirect ב-module.html
- [ ] H10: להוסיף `auth-modal.js` ל-reset-password.html
- [ ] M2: `defer` לסקריפטים ב-practice.html
- [ ] M3: לכלול tags בחיפוש ספרייה

### חבילה 3 — סנכרון תקין (1.5 שעות)
- [ ] H3: סנכרון השלמת שיעור ל-Supabase
- [ ] H4: סנכרון הערות (גם sync וגם export)
- [ ] H6: לתקן הספירה המתנפחת של attempts
- [ ] H7: לאפס streak אם lastDate ישן
- [ ] H8: מעבר במבחן = השלמה ב-course_progress

### חבילה 4 — חוויה (1 שעה)
- [ ] H2: "דלג לשיעור הבא" שבאמת מדלג
- [ ] H9: getBasePath בלינק שכחתי סיסמה
- [ ] H11: 45 שניות רק כשהוידאו באמת רץ
- [ ] H12: stub ל-bwcAuth כשנכשל

### חבילה 5 — שיופים (3 שעות)
- כל הבינוניים והנמוכים

---

## הערה לחבירה עם Claude Chrome
תוצאות Claude Chrome (מהדפדפן) יבואו עם פוקוס על:
- חוויה ויזואלית במובייל ובדסקטופ
- כפתורים שתקועים / לא נראים
- בעיות RTL / קונטרסט / קריאות

האודיט הזה משלים — שני מבטים שונים. שילוב של שניהם נותן כיסוי מלא.
